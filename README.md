# SudokuSolver
A program that solves sudoku puzzles

uses the module TKinter for the GUI, install with this command if needed ```sudo apt-get install python3-tk```
    >See documentation here: https://docs.python.org/3/library/tkinter.html